<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="kz.kazpost.unloadingarea.ui.AuthFragment"
        tools:layout="@layout/fragment_auth"
        android:label="Авторизация" >
        <action
            android:id="@+id/action_authFragment_to_transportFragment"
            app:destination="@id/transportFragment" />
    </fragment>

    <fragment
        android:id="@+id/transportFragment"
        android:name="kz.kazpost.unloadingarea.ui.TransportFragment"
        android:label="TransportFragment" />


</navigation>